# =============================================================================
# ComfyUI Minimal Requirements - For NGC PyTorch Base
# =============================================================================
# NOTE: NGC PyTorch 25.12 already provides:
#   - torch, torchvision (2.10.0a0)
#   - triton
#   - numpy
#   - Pillow
#   - scipy
# NOTE: torchaudio is NOT available - NGC lacks stable C API headers for
# source build, and pre-built wheels have ABI mismatch with PyTorch 2.10.0a0.
# Audio nodes (nodes_audio.py, nodes_audio_encoder.py) are removed in Dockerfile.
# =============================================================================

# ComfyUI Frontend & Templates (official packages)
comfyui-frontend-package>=1.35.9
comfyui-workflow-templates>=0.7.64
comfyui-embedded-docs>=0.3.1

# Core ComfyUI dependencies (from official requirements.txt)
torchsde
transformers>=4.50.3
tokenizers>=0.13.3
einops
sentencepiece
safetensors>=0.4.2
aiohttp>=3.11.8
yarl>=1.18.0
pyyaml
tqdm
psutil
alembic
SQLAlchemy
av>=14.2.0
kornia>=0.7.1
spandrel
pydantic>=2.0
pydantic-settings>=2.0

# Additional dependencies for full functionality
huggingface-hub>=0.20.0
accelerate>=0.29.0
diffusers>=0.33.0
